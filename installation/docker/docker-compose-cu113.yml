services:
  bev-train:
    container_name: bevfusionx-cu113
    build:
      context: .
      dockerfile: Dockerfile.cu113
    image: bevfusionx:cu113
    working_dir: /workspace
    volumes:
      - /home/${USER}/docker/bevfusionx-cu113:/workspace
    stdin_open: true
    tty: true
    command: bash
    # GPU support (Compose v2 with NVIDIA Container Toolkit)
    gpus: all
    # Shared memory
    shm_size: "32gb"
