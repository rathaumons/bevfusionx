services:
  bev-train:
    container_name: bev-train-cu130
    build:
      context: .
      dockerfile: Dockerfile.cu130
    image: bev-train:cu130
    working_dir: /workspace
    volumes:
      - /home/${USER}/docker/bev_train_cu130:/workspace
    stdin_open: true
    tty: true
    command: bash
    # GPU support (Compose v2 with NVIDIA Container Toolkit)
    gpus: all
    # Shared memory
    shm_size: "32gb"
