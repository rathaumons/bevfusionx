services:
  bev-train:
    container_name: bev-train-cu128
    build:
      context: .
      dockerfile: Dockerfile.cu128
    image: bev-train:cu128
    working_dir: /workspace
    volumes:
      - /home/${USER}/docker/bev_train_cu128:/workspace
    stdin_open: true
    tty: true
    command: bash
    # GPU support (Compose v2 with NVIDIA Container Toolkit)
    gpus: all
    # Shared memory
    shm_size: "32gb"
